import { View, Text } from "react-native";
import React from "react";

const Chat = (props) => {
    return (
        <View>
            <Text>ChatScreen</Text>
        </View>
    );
};

export default Chat;
