import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ReadingModule = () => {
  return (
    <View>
      <Text>ReadingModule</Text>
    </View>
  );
};

export default ReadingModule;

const styles = StyleSheet.create({});
