import { StyleSheet, Text, View } from "react-native";
import React from "react";

const QuestionModule = () => {
  return (
    <View>
      <Text>QuestionModule</Text>
    </View>
  );
};

export default QuestionModule;

const styles = StyleSheet.create({});
