import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MediaModule = () => {
  return (
    <View>
      <Text>MediaModule</Text>
    </View>
  );
};

export default MediaModule;

const styles = StyleSheet.create({});
